{
  "name": "presenting_me",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal local app that runs without external dependencies",
  "scripts": {
    "start": "node server.js",
    "lint": "node --check server.js"
  }
}
